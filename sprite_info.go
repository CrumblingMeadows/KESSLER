package main

var (
	spriteimg []Asset

// spriteimg[0] = Asset{"tile01","0000_core",tile01}
)

func init() {
	/*
		for i:=0;i<len(spriteimg);i++{
			spriteimg[i].sprite_img=ebiten.NewImageFromImage()///find assetid, & find sprite with corresponding ID ALTERNATIVELY create asset via corresponding sprite ID
		}
	*/
}
